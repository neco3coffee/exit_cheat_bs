"use client";
import styles from "./index.module.scss";

const Header = () => {
  return (
    <>
      <header className={`${styles.header}  `}>SafeBrawl</header>
    </>
  );
};

export default Header;
